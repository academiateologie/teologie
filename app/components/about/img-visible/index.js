import React from "react";
import styles from "./img.visible.module.scss";
import { motion } from "framer-motion";

const Index = () => {
  return (
    <div
      className={styles.about__image}
    ></div>
  );
};

export default Index;
